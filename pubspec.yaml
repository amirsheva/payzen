name: payzen
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.1 <4.0.0' # <-- این بخش مهم فراموش شده بود

dependencies:
  flutter:
    sdk: flutter
  
  flutter_secure_storage: ^9.2.2  

  cupertino_icons: ^1.0.8
  dio: ^5.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true